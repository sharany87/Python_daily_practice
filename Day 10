Question: Write a Python program to count the number of consonants in a given string.
text = input("Enter a string: ")

count = 0

for ch in text:
    if ch.isalpha() and ch not in "aeiouAEIOU":
        count += 1

print(count)
